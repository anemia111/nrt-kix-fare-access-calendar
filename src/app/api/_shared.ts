/**
 * API ルートで共有する定数。
 * `_` 始まりのため、Next.js のルートとしては扱われない。
 */

export { CALENDAR_DAYS } from "@/domain/routes";
import { ROUTE_IDS } from "@/domain/types";

export const ROUTE_IDS_LIST: readonly string[] = ROUTE_IDS;
